generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Homestay {
  id        String @id @default(cuid())
  name      String
  location  String
  price     Int
}